# ContextTrees

Macro/micro context system for persisting project-level design context and
assembling ephemeral working sessions from it. Feeds into Naulon.

See [docs/macro-micro-context-spec.md](docs/macro-micro-context-spec.md) for
the working spec: layer overview, session lifecycle, and how updates flow
back into agent memory and the design map.
